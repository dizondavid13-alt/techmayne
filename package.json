{
  "name": "techmayne",
  "version": "1.0.0",
  "description": "TechMayne chatbot system",
  "scripts": {
    "start": "cd backend && npm install && npm start",
    "install": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
